coverage/
build/
dist/
api-docs/
browser-test/

# Standalone, dependency-free benchmark scripts (run directly by node/xst, not
# part of any package's build or test graph). They use engine globals
# (`print`) and hand-rolled loops that the shared config would flag.
/packages/immutable-arraybuffer/benchmarks/

*.json

# see .gitignore

*.d.ts
*.d.cts
*.d.mts
!*.types.d.ts
!*.types.d.mts
!*.types.d.cts

# tseslint can become confused by these files; attempt to remove this when upgrading eslint/tseslint
*.types.js
