# #3154: support dynamic imports in CJS

- **URL:** https://github.com/endojs/endo/pull/3154
- **State:** OPEN
- **Author:** @boneskull
- **Created:** 2026-04-01T01:08:54Z
- **Updated:** 2026-04-23T01:42:36Z
- **Closed:** <no value>
- **Merged:** <no value>
- **Base:** master
- **Head:** boneskull/cjs-imports
- **Draft:** true
- **Files:** 13 (+241 −14)
- **Labels:** `enhancement`, `lavamoat`, `ecosystem-compatibility`

## Summary

- **What:** Draft adding dynamic `import()` support in CJS sources via `parse-cjs-shared-export-wrapper.js` (injected `importFn` calling `compartment.import()`) and extends `@endo/cjs-module-analyzer.analyzeCommonJS` to return `{imports, source}` rewriting `import()` to `$h_import()` for SES evasion.
- **Why:** CJS modules in `@endo/compartment-mapper` could not use dynamic imports; non-exit dynamics still need `_preload`/`captureForMap` support but exits work given an `importHook`.
- **Themes:** compartment-mapper, ecosystem-compat
- **Status:** draft

## Description

## REVIEWERS

I acknowledge mauling the CJS lexer to transform `import()` calls on-the-fly is an egregious violation of the single-responsibility principle and is thus a _bad idea._  That said, I want to replace the lexer with an AST parser (done similarly to ESM sources), perhaps soon.  

### feat(compartment-mapper): support dynamic import() in CommonJS

This change enables use of (`await`) `import(specifier)` in CommonJS sources.

To make this work, `parse-cjs-shared-export-wrapper.js` provides an `importFn` to the functor, which invokes `compartment.import()`.  The CJS module source is transformed on-the-fly by `@endo/cjs-module-analyzer` which evades SES.

Note that dynamically-imported _non-exit_ modules are still unsupported by `loadLocation`/`importLocation` due a lack of support for pre-loaded compartments (`_preload`). Currently, only `captureForMap()` supports this option. Without it, dynamically-imported modules will be unknown to Endo unless they were statically loaded via other means.  Exit modules can be loaded dynamically _if_ the caller provides an `importHook`.

Added tests covering dynamic imports in _both_ ESM and CJS.

### feat(cjs-module-analyzer): detect dynamic imports; add on-the-fly evasion

This modifies the `analyzeCommonJS` function to return an object with two new properties:

1. `imports`: list of specifiers imported via `import()` calls
2. `source`: transformed source for SES evasion, replacing `import()` with `$h_import()`

If `$h_import` is detected in the original source, it will throw an exception. There are better ways to handle this, of course.

Updated `README.md` and added missing MIT license / attribution.


## Commits (2)

- `58653a66b7e21cabf5e13af2a938e27a77b647b3` feat(cjs-module-analyzer): detect dynamic imports; add on-the-fly eva…
- `048d0980349a3d1a3e8e0a11faa0339b3b126185` feat(compartment-mapper): support dynamic import() in CommonJS

## Reviews (1)

---

### @copilot-pull-request-reviewer — COMMENTED — 2026-04-04T02:26:44Z

## Pull request overview

Adds support for dynamic `import()` in CommonJS modules by rewriting CJS sources to call an injected import function, and extends the CJS analyzer to detect/collect dynamic import specifiers and provide a transformed source for SES evasion.

**Changes:**
- Extend `@endo/cjs-module-analyzer` to detect `import()` calls, return discovered specifiers, and rewrite `import()` to `$h_import()`.
- Update `compartment-mapper` CJS parsing/wrapping to inject `$h_import` that delegates to `compartment.import()`, and include dynamic-import specifiers in the dependency list.
- Add fixtures + tests covering dynamic imports in both ESM and CJS (with archive expectations marked as known failures where applicable).

### Reviewed changes

Copilot reviewed 8 out of 13 changed files in this pull request and generated 4 comments.

<details>
<summary>Show a summary per file</summary>

| File | Description |
| ---- | ----------- |
| packages/compartment-mapper/src/parse-cjs.js | Injects `$h_import` into the CJS functor and merges dynamic-import specifiers into `record.imports`; now returns transformed bytes. |
| packages/compartment-mapper/src/parse-cjs-shared-export-wrapper.js | Adds `importFn` that resolves via `compartment.import()` (with `resolvedImports` mapping). |
| packages/compartment-mapper/test/cjs-compat.test.js | Adds a CJS dynamic-import scaffold test using an `importHook`. |
| packages/compartment-mapper/test/dynamic-import-esm.test.js | Adds an ESM dynamic-import scaffold test. |
| packages/compartment-mapper/test/fixtures-cjs-compat/node_modules/dynamic-import/package.json | Adds a CJS fixture package for dynamic import testing. |
| packages/compartment-mapper/test/fixtures-cjs-compat/node_modules/dynamic-import/index.js | CJS fixture exporting a function that calls `import('node:fs')`. |
| packages/compartment-mapper/test/fixtures-dynamic-import-esm/node_modules/app/package.json | Adds an ESM fixture package for dynamic import testing. |
| packages/compartment-mapper/test/fixtures-dynamic-import-esm/node_modules/app/index.js | ESM fixture that dynamically imports `./foo.js`. |
| packages/compartment-mapper/test/fixtures-dynamic-import-esm/node_modules/app/foo.js | ESM fixture module providing a default export. |
| packages/cjs-module-analyzer/index.js | Implements dynamic `import()` detection, `$h_import` rewrite, and adds `imports`+`source` to the analysis result. |
| packages/cjs-module-analyzer/test/cjs-module-analyzer.test.js | Adds tests for `import()` detection/rewrite and `$h_import` reservation behavior. |
| packages/cjs-module-analyzer/README.md | Documents the fork and newly reported dependency types. |
| packages/cjs-module-analyzer/LICENSE-MIT | Adds upstream MIT license attribution. |
</details>






---

💡 <a href="/endojs/endo/new/master?filename=.github/instructions/*.instructions.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add Copilot custom instructions</a> for smarter, more guided reviews. <a href="https://docs.github.com/en/copilot/customizing-copilot/adding-repository-custom-instructions-for-github-copilot" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn how to get started</a>.


## Comments (2)

---

### @boneskull (MEMBER) — 2026-04-01T01:08:56Z

<!-- gh-stack:nav -->
### :books: Pull Request Stack

- branch: `master`
  - **[support dynamic imports in CJS #3154](https://github.com/endojs/endo/pull/3154)** - branch: `boneskull/cjs-imports` *(this PR)*

---
*Managed by [gh-stack](https://github.com/boneskull/gh-stack)*


---

### @changeset-bot (NONE) — 2026-04-01T01:09:01Z

###  ⚠️  No Changeset found

Latest commit: 048d0980349a3d1a3e8e0a11faa0339b3b126185

Merging this PR will not cause a version bump for any packages. If these changes should not result in a new version, you're good to go. **If these changes should result in a version bump, you need to add a changeset.**

<details><summary>This PR includes no changesets</summary>

  When changesets are added to this PR, you'll see the packages that this PR includes changesets for and the associated semver types

</details>

[Click here to learn what changesets are, and how to add one](https://github.com/changesets/changesets/blob/main/docs/adding-a-changeset.md).

[Click here if you're a maintainer who wants to add a changeset to this PR](https://github.com/endojs/endo/new/boneskull/cjs-imports?filename=.changeset/selfish-socks-reply.md&value=---%0A%22%40endo%2Fcjs-module-analyzer%22%3A%20patch%0A%22%40endo%2Fcompartment-mapper%22%3A%20patch%0A---%0A%0Asupport%20dynamic%20imports%20in%20CJS%0A)



