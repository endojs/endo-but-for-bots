# #2793: refactor(pass-style,...): abstract symbol making to prepare for flip

- **URL:** https://github.com/endojs/endo/pull/2793
- **State:** MERGED
- **Author:** @erights
- **Created:** 2025-05-05T21:13:10Z
- **Updated:** 2025-06-03T22:33:38Z
- **Closed:** 2025-05-24T17:59:07Z
- **Merged:** 2025-05-24T17:59:07Z
- **Base:** master
- **Head:** markm-abstract-symbol-construction
- **Draft:** false
- **Files:** 26 (+128 −59)
- **Labels:** `next-release`

## Summary

- **What:** Pure refactor across `@endo/pass-style` and friends introducing `passableSymbolForName(name)` (replaces `Symbol.for`) and `unpassableSymbolForName(name)` (replaces `Symbol(name)` in tests) at every passable-symbol callsite.
- **Why:** Sets up the impending change to passable-symbol representation (refs #2792, #2777) without altering current behavior — registered symbols don't GC on V8.
- **Themes:** pass-style, ocapn, chore
- **Status:** merged

## Description

Closes: #XXXX
Refs: #2792 #2777

## Description

This PR is essentially a pure refactor to set things up for upcoming substantive PRs like #2792 . The main one will change the passable symbol representation, along the lines of #2777 but likely different. This is for symbols used for purposes passing, or test counterexamples to being passable. All other symbol uses should not change.

The change from this PR is
- change calls to `Symbol.for('foo')` for making a passable symbol, to `passableSymbolForName('foo')`
- change calls to `Symbol('foo')` for making a non-passable symbol for testing purposes, to `unpassableSymbolForName('foo')`

### Security Considerations

Assuming we changed each symbol-making callsite from the old to the correct new one, and assuming that we did not alter the code that was making symbols for other reasons, none.

### Scaling Considerations

Using registered symbols like `Symbol.for('foo')` is a semantically perfect binding to the pass-style and ocapn category `'symbol'`. However its scaling properties are disastrous. In some js systems like v8, unused registered symbols are not garbage collected. This is why we're going to switch passable symbol representation. However this PR does none of that yet. It merely sets the stage. This PR should not cause any change in scaling.

### Documentation Considerations

The need to pass symbols is rare and obscure, and most users will not need to be aware of this. However, any reference documentation should clearly explain when to use the new abstract symbol-creation functions, and when not to.

### Testing Considerations

As a pure refactor, the main concern is that all the old tests still pass. Any substantive new tests or test changes will be downstream, in the PRs that this PR enables.

### Compatibility Considerations

Assuming we changed each symbol-making callsite from the old to the correct new one, and assuming that we did not alter the code that was making symbols for other reasons, none.

### Upgrade Considerations

As a pure refactor, none. Beyond that, when we change the JS representation of a passable symbol, we will not change the wire representation in any of our encodings. Thus, durable state saved using the representation will, at that time, revive into the new representation, so still none. Likewise with messages sent from code on one side of the upgrade and received by code on the other side of the upgrade.


## Commits (3)

- `7f78a07e2a23cb8e6627834f6533780324e8abfd` refactor(pass-style,...): abstract symbol construction to prepare for…
- `ae9d7afe8633c5afa879cfefc78ea20e658d4784` fixup! format
- `070f9679a61074f74335d90f916d24cc556dab2c` Merge branch 'master' into markm-abstract-symbol-construction

## Reviews (6)

---

### @erights — COMMENTED — 2025-05-05T21:21:36Z



---

### @erights — COMMENTED — 2025-05-05T21:24:10Z



---

### @kriskowal — APPROVED — 2025-05-23T21:53:30Z



---

### @Ebisaki — COMMENTED — 2025-06-03T21:45:37Z

``0x4200000000000000000000000000000000000042

---

### @Ebisaki — COMMENTED — 2025-06-03T21:48:28Z

packages/ocapn/src/codecs/passable.js

---

### @Ebisaki — COMMENTED — 2025-06-03T21:48:45Z




## Comments (6)

---

### @Ebisaki (NONE) — 2025-06-03T21:39:50Z

> پایان#۲۱۸۳۸۸ مراجع: [#2792 ](https://github.com/endojs/endo/pull/2792) #2777
> 
> ## توضیحات
> این PR اساساً یک اصلاح‌کننده‌ی صرف برای تنظیم موارد برای PRهای اساسی آینده مانند [#۲۷۹۲](https://github.com/endojs/endo/pull/2792) است . مورد اصلی، نمایش نماد قابل قبول را تغییر می‌دهد، در امتداد [#۲۷۷۷](https://github.com/endojs/endo/pull/2777) اما احتمالاً متفاوت. این برای نمادهایی است که برای اهداف عبور یا آزمایش مثال‌های نقض برای قابل قبول بودن استفاده می‌شوند. سایر کاربردهای نماد نباید تغییر کنند.
> 
> تغییر از این روابط عمومی این است
> 
> * `Symbol.for('foo')`برای ایجاد یک نماد قابل قبول، فراخوانی‌های را تغییر دهید`passableSymbolForName('foo')`
> * تغییر فراخوانی‌ها `Symbol('foo')`برای ایجاد یک نماد غیرقابل عبور برای اهداف آزمایشی، به`unpassableSymbolForName('foo')`
> 
> ### ملاحظات امنیتی
> با فرض اینکه ما هر سایت فراخوانی نمادسازی را از قدیمی به جدید صحیح تغییر داده باشیم، و با فرض اینکه کدی را که نمادسازی می‌کرد به دلایل دیگر تغییر نداده باشیم، هیچ.
> 
> ### ملاحظات مقیاس‌بندی
> استفاده از نمادهای ثبت‌شده مانند []، `Symbol.for('foo')`یک اتصال بی‌نقص از نظر معنایی برای دسته‌های pass-style و ocapn است `'symbol'`. با این حال، ویژگی‌های مقیاس‌بندی آن فاجعه‌بار است. در برخی از سیستم‌های js مانند v8، نمادهای ثبت‌شده استفاده‌نشده، جمع‌آوری زباله نمی‌شوند. به همین دلیل است که ما قصد داریم نمایش نمادهای قابل قبول را تغییر دهیم. با این حال، این PR هنوز هیچ‌کدام از این کارها را انجام نمی‌دهد. این صرفاً زمینه را فراهم می‌کند. این PR نباید هیچ تغییری در مقیاس‌بندی ایجاد کند.
> 
> ### ملاحظات مستندسازی
> نیاز به ارسال نمادها نادر و مبهم است و اکثر کاربران نیازی به آگاهی از این موضوع نخواهند داشت. با این حال، هر گونه مستندات مرجع باید به وضوح توضیح دهد که چه زمانی از توابع جدید ایجاد نماد انتزاعی استفاده شود و چه زمانی استفاده نشود.
> 
> ### ملاحظات آزمایش
> به عنوان یک ریفکتور خالص، نگرانی اصلی این است که تمام تست‌های قدیمی همچنان با موفقیت پشت سر گذاشته شوند. هرگونه تست جدید یا تغییر اساسی در تست‌ها، در PRهایی که این PR فعال می‌کند، اعمال خواهد شد.
> 
> ### ملاحظات سازگاری
> با فرض اینکه ما هر سایت فراخوانی نمادسازی را از قدیمی به جدید صحیح تغییر داده باشیم، و با فرض اینکه کدی را که نمادسازی می‌کرد به دلایل دیگر تغییر نداده باشیم، هیچ.
> 
> ### ملاحظات ارتقا
> به عنوان یک refactor خالص، هیچ تغییری ایجاد نمی‌شود. فراتر از آن، وقتی نمایش JS یک نماد قابل قبول را تغییر می‌دهیم، نمایش wire را در هیچ یک از کدگذاری‌های خود تغییر نخواهیم داد. بنابراین، حالت پایدار ذخیره شده با استفاده از نمایش، در آن زمان در نمایش جدید احیا می‌شود، بنابراین هنوز هیچ تغییری ایجاد نمی‌شود. به همین ترتیب، پیام‌هایی که از کد در یک طرف ارتقاء ارسال می‌شوند و توسط کد در طرف دیگر ارتقاء دریافت می‌شوند.



---

### @Ebisaki (NONE) — 2025-06-03T21:46:37Z

Ok

---

### @Ebisaki (NONE) — 2025-06-03T21:47:27Z

#2832 

---

### @Ebisaki (NONE) — 2025-06-03T21:47:33Z

https://github.com/endojs/endo/pull/2793#issuecomment-2937308543

---

### @Ebisaki (NONE) — 2025-06-03T21:49:15Z

### ****

---

### @Ebisaki (NONE) — 2025-06-03T21:49:34Z

packages/far/test/e.test.js

