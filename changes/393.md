# #393: feat(ses): Add support for third-party modules

- **URL:** https://github.com/endojs/endo/pull/393
- **State:** MERGED
- **Author:** @kriskowal
- **Created:** 2020-07-28T18:43:15Z
- **Updated:** 2020-07-29T18:21:17Z
- **Closed:** 2020-07-29T18:21:16Z
- **Merged:** 2020-07-29T18:21:16Z
- **Base:** master
- **Head:** kris/third-party
- **Draft:** false
- **Files:** 8 (+812 −54)
- **Labels:** 

## Summary

- **What:** Decouples `ses` Compartment module `load` and `execute` phases so non-ESM (third-party / CJS / WASM-shaped) static module records can participate: `load` only inspects superficial `imports`, `execute` dispatches by record type, and CJS exports surface via post-execute property enumeration without ESM-style live bindings.
- **Why:** Lets `ses` host CommonJS and similar third-party module shapes alongside ESM in a Compartment graph.
- **Themes:** ses-lockdown, compartments, ecosystem-compat
- **Status:** merged

## Description

This change includes a sequence of refactoring commits that fully decouple the `load` and `execute` phases of importing modules. The `load` phase only considers the superficial `imports` field of a static module record, which is the same for both first-party ESM and third-party static module records. The `execute` phase then treats these types of static module record according to their type.

In validating a static module record, we take care to provide the most informative error message, particularly to distinguish the case where a `StaticModuleRecord` may have crossed realms and the SES-shim could not see its “internal” fields. This is distinguishable because such records have `imports` and no `execute` method, implying that there *should* be internal fields associated with the record.

non-ESM modules do not support late/live binding, as this is not required for WASM and very unlikely to be necessary for CommonJS modules. This change slightly complicates ESM module instance implementation to take into account importing and exporting names from modules that do not have notifiers and whose exports must be observed by enumerating the properties of the exports object after the module runs to completion. After executing a third-party module, mutations to its exports namespace are not observable from any ESM modules that import them. Those mutations are however observable on the exports namespace object (as returned by dynamic `import`, `module`, and `importNow`) and in other third-party modules that retain that reference directly.

This change includes two layers of tests. The first layer verifies the static module record interface usage directly, using mock static module records. The second layer considers the case of a CommonJS static module record and covers its special treatment of the default export. This leaves open the possibility of misbehavior in the supremely unlikely chance of a CommonJS module that assigns an export to `exports.default` directly.

## Commits (7)

- `d74e5dc6d142c80885ebc62e7cac8f108dcb159b` Use SMR imports directly
- `db5a486e63cae9ec3443addc3fa1c58ec8e6ec9a` Couple module analysis closer to module instance
- `707abdb38a252926dbed9478f18c2fcb14f99fb3` Unwind module analyses from load phase
- `38c8804f2e6d77a66fbd3d7a6f6e4dfd9da64b50` Implement third-party module linkage
- `735d25a7c205d7cc6319f5c2e6d7d5dc612a6016` Document third-party static module records
- `254a0ab36bd0052e5e361fe33bce6507eac858cb` Add CommonJS test
- `f18cac3f6e93942e7c480c4f801986074fc4d008` Clarify static module record errors

## Reviews (2)

---

### @erights — APPROVED — 2020-07-29T05:46:46Z

LGTM!

---

### @kriskowal — COMMENTED — 2020-07-29T17:55:06Z




## Comments (3)

---

### @erights (CONTRIBUTOR) — 2020-07-28T19:44:59Z

>  This leaves open the possibility of misbehavior in the supremely unlikely chance of a CommonJS module that assigns an export to exports.default directly.

Please say more about this? What are the implied vulnerabilities?

---

### @kriskowal (MEMBER) — 2020-07-28T21:20:54Z

> > This leaves open the possibility of misbehavior in the supremely unlikely chance of a CommonJS module that assigns an export to exports.default directly.
> 
> Please say more about this? What are the implied vulnerabilities?

Digging further, the relationship among CommonJS dependencies is preserved. The following example will work. One module late-writes and another module later-reads.

```js
setTimeout(() => {
  exports.foo = 10;
}, 500);
```

The effect of assigning to `foo` in a turn after the module finishes executing is observable to any module that requires the exports object and lazy binds `foo`.

```js
const other = require('module-above');
setTimeout(() => {
  console.log(other.foo);
}, 1000);
```

However, if the latter module is implemented as an ESM, `foo` will permanently reflect the undefined initial value and will not be updated after the timer in the first module fires. There *might* be some extant code where this worked before, but it is vanishingly unlikely.

```js
import { foo } from 'module-above';
setTimeout(() => {
  console.log(foo);
});
```

---

### @kriskowal (MEMBER) — 2020-07-28T21:22:03Z

Oh, note that `export * from 'some.cjs'` *does* work.

