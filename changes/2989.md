# #2989: feat(harden): Surface harden before lockdown

- **URL:** https://github.com/endojs/endo/pull/2989
- **State:** OPEN
- **Author:** @kriskowal
- **Created:** 2025-10-10T17:20:26Z
- **Updated:** 2026-01-10T15:46:29Z
- **Closed:** <no value>
- **Merged:** <no value>
- **Base:** master
- **Head:** kriskowal-endo-harden-shallow
- **Draft:** false
- **Files:** 28 (+1905 −0)
- **Labels:** 

## Summary

- **What:** Re-introduces `@endo/harden` with three modes (`hardened` condition for post-lockdown, default `shallow` surface freezing, `unsafe`/`noop-harden` for performance parity); all modes adopt the global/intrinsic `harden` after `lockdown` and cause `lockdown` to fail if `harden` was already used.
- **Why:** Toward #2983 — decouple Hardened Modules from `lockdown` while retaining bundle-size and integrity options.
- **Themes:** ses-lockdown
- **Status:** open

## Description

Refs: #2983

## Description

This change introduces an `@endo/harden` package that has three modes, as documented in the new README.

**post-lockdown** All of these modes adopt the behavior of the global or intrinsic `harden` after `lockdown`. All of these modes cause `lockdown` to fail if `harden` is used before `lockdown`.

**presume post-lockdown** In order to minimize bundle size increase, the `-C hardened` bundler condition can omit a user code implementation of `harden` and rely on the global environment, failing if the global environment lacks a `harden` global or intrinsic.

**shallow** The default behavior before lockdown is to transitively freeze own properties and to not traverse prototype chains. This makes the mode suitable for non-lockdown environments that tend to make uncoordinated changes to shared intrinsics, but preserves a sense of hardening for shallow immutability.

**unsafe** In order to use Hardened Modules without the performance penalty of even a shallow freeze, electing `-C harden:unsafe` will provide a fake `harden` implementation that freezes nothing.

### Security Considerations

Each of these modes provides different security footing.

### Scaling Considerations

Build conditions are provided to compensate for difficulties scaling, both for performance and bundle size increases consequent on adopting `@endo/harden` for a hardened module.

### Documentation Considerations

The new package’s README is comprehensive.

### Testing Considerations

All modes are covered. The shortfall in coverage mostly pertains to shimmed behavior like `hasOwn`.

### Compatibility Considerations

Adopting `@endo/harden` will, by itself, increase bundle sizes. We consider it acceptable to obligate applications with sensitive bundle sizes to also adopt `-C hardened` in their build procedures to mitigate the increase.

This overall effect of this change is to increase the portability of Hardened modules with frontend and testing environments that require uncoordinated modification to shared intrinsics.

### Upgrade Considerations

None.

## Commits (24)

- `05e45d04c20aa4eef5bdd3cf6619c5035eb3e3f9` feat(ses): Sense @endo/harden usage before lockdown
- `eb9f4e67f43182fc650d88017dcc467616662e2b` chore(harden): Create from scaffold
- `f4edc84ebe4bd3804f6eb2f2f946d812e1161809` refactor(harden): Remove scaffold dross
- `c7daa4872834a8138c776d78203597cc4a7ad5be` refactor(harden): Duplicate ses make-hardener.js without modification
- `6f42bff7c175f61cbcbbbde89f4230e836ed525b` refactor(harden): Relive makeHardener of assert dependency
- `6e78e865737041d3856d12418356d0c600bf44fd` refactor(harden): Make traversePrototypes option of makeHardener
- `82f1390e012857cbfc4c8d0bafbfde3e7875dcbe` refactor(harden): Inline ses commons.js into make-hardener.js without…
- `9a7913a8b8f223c429108bd91b6594f807c5f5e2` refactor(harden): Do not export inline commons of make-harden.js
- `330be16a18a016ef2ae350cf86e955256f32011e` refactor(harden): Inline Harden type in make-hardener.js
- `675d89ac4a4bce509b4b76080ff885a9fde8c8a4` refactor(harden): Remove unused commons in make-hardener
- `6bd3ba16a357de94fc69200e9d4dca20f639a913` feat(harden): Add mechanism for detecting, providing, installing glob…
- `5efeef4d36ac6813a2eb705ccc77fa36bdf5aa93` feat(harden): Presumed hardened mode
- `8768d77be923c924b8d5c19b0a107340ac6acf41` feat(harden): Unsafe harden mode
- `28e7bc0358d252693cb91e7424920267a3234226` feat(harden): Surface harden mode
- `ff33e294438922e22feae4e9891f5158fbf69e73` feat(harden): Add surface harden
- `199ce553c345c78b96b642a70111de5fe43a75fe` feat(harden): Export three modes
- `8fa45e36ba75fcc7e4f6d4fd99fab008960636a3` test(harden): Test all modes
- `4a2af4f908290b059630c7cb51dced6f03d63163` refactor(harden): Copy ses make-hardener.test.js without modification
- `f1f56efa6edd17ec71e5b01e42a98b72cc12366e` refactor(harden): Adapt make-hardener test
- `6aded1a2748eb161ee03e454da6c646d5a41dc76` refactor(harden): Duplicate make-hardener test for shallow
- `f9ed45a9305d5a71c805a70a920864580b107a20` refactor(harden): Adapt shallow make-hardener test
- `e82e0af30978cc63ca2cfb1b0b367667db99644b` fix(harden): Improve Harden type
- `e44bd029b4c6febc6dae4fa151637b2ba5160204` doc(harden): README
- `7c6ad10f0dc5851dc169c2a57ffaa8a4d10b785b` chore: Update yarn.lock

## Reviews (0)


## Comments (1)

---

### @kriskowal (MEMBER) — 2025-10-20T21:29:08Z

Note to self: when I pick this up again, @erights and I are in agreement to use the term “surface” rather than “shallow”, as opposed to “volume”.

