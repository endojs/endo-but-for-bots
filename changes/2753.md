# #2753: feat(bundle-source,import-bundle): Thread importHook option to endoZipBase64 moduleFormat.

- **URL:** https://github.com/endojs/endo/pull/2753
- **State:** MERGED
- **Author:** @grypez
- **Created:** 2025-04-02T17:59:41Z
- **Updated:** 2025-04-02T21:43:05Z
- **Closed:** 2025-04-02T21:43:04Z
- **Merged:** 2025-04-02T21:43:04Z
- **Base:** master
- **Head:** grypez/thread-import-hook-to-zip-base64
- **Draft:** false
- **Files:** 9 (+96 −1)
- **Labels:** 

## Summary

- **What:** Threads the `importHook` option from `@endo/compartment-mapper` through `@endo/bundle-source` and `@endo/import-bundle` for the `endoZipBase64` format, so bundles can resolve external host dependencies (e.g., `fs`, `path`) at import time.
- **Why:** Until now `endoZipBase64` could not encode external exits even though the underlying compartment mapper supported them.
- **Themes:** bundle-source, compartment-mapper
- **Status:** merged

## Description

<!-- < < < < < < < < < < < < < < < < < < < < < < < < < < < < < < < < < ☺
v                               ✰  Thanks for creating a PR! ✰
☺ > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > >  -->

## Description

This PR allows `bundleSource` and `importBundle` to bundle files and import bundled files respectively in the case that
1. the source file depends on external dependencies (such as 'fs' and 'path') and
2. the bundle is created using the endoZipBase64 moduleFormat option

This functionality already existed in `@endo/compartment-mapper`, and this PR simply plumbs the importHook option to the bundleSource and importBundle APIs.

### Security Considerations

Any designs which depend upon code bundled with the endoZipBase64 moduleFormat to have no external dependencies will be broken. To my knowledge none exist.

### Scaling Considerations

None

### Documentation Considerations

The bundleSource documentation does not have a use case showing the importHook option.

### Testing Considerations

See included tests.

### Compatibility Considerations

None

### Upgrade Considerations

Bundling with endoZipBase64 is now more permissive, so existing importBundle calls may fail in a way they couldn't before.


## Commits (5)

- `e30441f07630949518d6df35535a56347a84b797` feat(bundle-source): Thread importHook option to endoZipBase64 format
- `e6c9ae04264e05bcb3280097d1e3da7bc3dc62e3` test(bundle-source): Add immediate and transitive fs exit cases
- `4d0616b7b5d787139a201290009e0b1623c39529` feat(import-bundle): Thread importHook option to endoZipBase64 format
- `8782109f8b82ce5e337c2b363be459a72896393c` test(import-bundle): Add a roundtrip test case for external fs
- `5dc8dc2bf366eaab46187682ffce687e6feb9e60` docs: bundle importHook NEWS

## Reviews (1)

---

### @kriskowal — APPROVED — 2025-04-02T19:12:31Z

Thank you, style nits only.


## Comments (0)

