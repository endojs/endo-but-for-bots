# #1061: Remove pureCopy, ALLOW_IMPLICIT_REMOTABLES

- **URL:** https://github.com/endojs/endo/pull/1061
- **State:** MERGED
- **Author:** @erights
- **Created:** 2022-02-04T06:22:59Z
- **Updated:** 2023-05-09T21:39:13Z
- **Closed:** 2022-02-04T18:08:48Z
- **Merged:** 2022-02-04T18:08:48Z
- **Base:** master
- **Head:** markm-kill-pureCopy
- **Draft:** false
- **Files:** 18 (+65 −291)
- **Labels:** 

## Summary

- **What:** Removes `pureCopy` and the `OnlyData`/`PureData` distinction from `@endo/marshal`, plus the long-deprecated `ALLOW_IMPLICIT_REMOTABLES` flag and `sameValueZero`.
- **Why:** Marshal can now assume CopyArray/CopyRecord/Tagged composites are not proxies (à la Records and Tuples), making `pureCopy` redundant; relies on agoric-sdk #4458 landing first.
- **Themes:** marshal, pass-style
- **Status:** merged

## Description

We are now allowing ourselves to *assume* a protection we have not yet implemented --- that Passable pass-by-copy composites (CopyArray, CopyRecord, Tagged) cannot be proxies. Enforcing this would mirror the security properties of the proposed Records and Tuples, ensuring that these are only passive pure data that cannot cause side effects or interleave with user code, for example, to mount reentrancy attacks. (Need issue number where this assumption is documented.)

Given that assumption, we no longer need `pureCopy` nor the contrast between `OnlyData` and `PureData`. The name `PureData` better suggests the strong property that we're allowing ourselves to assume. So we delete all uses of `pureCopy` and occurrences of `OnlyData` or replace them with `PureData`.

This PR also removes the long deprecated `ALLOW_IMPLICIT_REMOTABLES` flag / environment variable. We have long ago switched it off by default and have gone for a long time without any need to turn it back on. At this point, we won't break anything by removing it.

https://github.com/Agoric/agoric-sdk/pull/4458 should go first, deleting uses of `pureCopy` which endo will no longer export.

The only use of `sameValueZero` was in agoric-sdk, so between the two PRs, we're moving it there. The other PR should go first for this reason as well. This PR stops exporting it. https://github.com/Agoric/agoric-sdk/pull/4458 stops importing, instead defining its own.

## Commits (1)

- `9e1f037c0306c0d5fef7c14717649e2969950856` fix: remove pureCopy, ALLOW_IMPLICIT_REMOTABLES

## Reviews (1)

---

### @michaelfig — APPROVED — 2022-02-04T17:58:29Z

This is great!  Nice simplification.



## Comments (1)

---

### @erights (CONTRIBUTOR) — 2022-02-04T10:22:29Z

https://github.com/Agoric/agoric-sdk/pull/4458 is merged, so this one should no longer be blocked on that one.

