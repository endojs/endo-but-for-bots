# #1432: feat(stream): Pubsub

- **URL:** https://github.com/endojs/endo/pull/1432
- **State:** CLOSED
- **Author:** @kriskowal
- **Created:** 2023-01-07T23:40:34Z
- **Updated:** 2023-08-17T23:54:50Z
- **Closed:** 2023-08-17T23:54:49Z
- **Merged:** <no value>
- **Base:** kris-daemon-worker-endowments
- **Head:** kris-pubsub
- **Draft:** true
- **Files:** 4 (+216 −1)
- **Labels:** 

## Summary

- **What:** Inlines a temporary pubsub implementation in `@endo/daemon`/`@endo/stream` (`makeTopic`, `makePubSub`, `Sink`/`Spring` types) to back inbox followers ahead of an Endo notifier.
- **Why:** Needed by `endo inbox --follow` work; not yet a notifier replacement.
- **Themes:** daemon, eventual-send
- **Status:** closed-unmerged

## Description

In the course of implementing Endo CLI inbox followers, I needed a minimum viable pubsub topic. Endo doesn’t have `@agoric/notifiers` yet, but it does have streams. Given that I expect notifiers to eventually make their way up to Endo, I’d accept a recommendation to instead inline this feature in the Endo Daemon in anticipation of replacing the implementation with a notifier. However, I believe this implementation stands on its own at least pedagogically.

This change introduces a succinct implementation of pubsub in terms of streams (async iterators). The `makeTopic` function is a pubsub analog for `makePipe`, but is broadcast instead of unicast, and consequently loses the ability to connect forward- and back-pressure signals. Where `makePipe` returns an entangled pair of streams, `makeTopic` produces a single `publisher` stream and a `subscribe` function for any number of subscriber streams.

The key insight that unlocks this change is that `makeQueue` is not a suitable foundation for pubsub, but is a suitable pattern to apply toward a new `makePubSub` variation on the theme. This function returns the producer side of a queue (which I’m calling a sink) and a function for generating consumers (springs). Like an async queue, a pub sub pair is backed by an async linked list, but the publisher side willfully advances the read head, such that new subscriptions only see future values and historical values are forgotten. Then, each subscriber captures a cursor so it doesn’t forget any of the links since its subscription was created.

In order to disentangle the pub and subs, `makeTopic` uses a `nullQueue`, so each pub and sub is paired with either the sink or spring side of the null queue. Consequently, I’ve found it useful to narrow the types accepted by `makeStream` to `Sink` and `Spring`, since pubs and subs only present these narrower types.

## Commits (1)

- `2911f05004419ccf71311c8bbd33e0177eff3728` feat(daemon): Inline temporary pubsub implementation

## Reviews (8)

---

### @michaelfig — COMMENTED — 2023-01-11T18:51:31Z

Looks good!  I'm not approving yet until you have a chance to address my comments (possibly through rebuttal).

BTW, I don't think `@agoric/notifier` should become `@endo/notifier`.  I agree with providing something simple and robust in `@endo/stream` that could be used directly or later wrapped by `@agoric/notifier` to provide its current abstractions.

There seem to be some arbitrary gaps between `@agoric/notifier` Subscribers and `makeTopic` that could be smoothed over:
- the `subscribe` created by `makeTopic` should be remotable.  Because of that, we require adapter functions to handle the case of creating a near AsyncIterable from a far `subscribe`.  A near AsyncIterable can't be created by `E(subscribe)()` because we need near behaviour (not just passable data or remote behaviour).  `ai[Symbol.asyncIterator]()` is called immediately and cannot return a promise.
- `subscribeEach(subP)` would be the replacement I'm suggesting for the current `subscribe()` function.  In the future, we could have `subscribeLatest(subP)`

I made some suggestions to change `makePubSub` to also track the `headPromise`, not just the `tailPromise`.


---

### @kriskowal — COMMENTED — 2023-01-11T23:21:12Z



---

### @michaelfig — COMMENTED — 2023-01-12T23:39:49Z



---

### @kriskowal — COMMENTED — 2023-01-13T00:16:46Z



---

### @michaelfig — APPROVED — 2023-01-15T21:35:16Z

My comments are nitpicky, but I'd be fine with you landing this knowing that it still needs work to be usable across vats or a loopback captp.

Doing that work without changing the local API  might lead to finding some bugs in the marshal+captp system, since we haven't worked much with records of far functions as opposed to far objects.  That would be a worthwhile thing to do, but tackling it now is probably out of scope.


---

### @michaelfig — COMMENTED — 2023-06-07T02:05:39Z

Suggestions for compatible API alignment.

---

### @kriskowal — COMMENTED — 2023-06-07T17:32:48Z



---

### @michaelfig — COMMENTED — 2023-06-07T17:44:18Z




## Comments (7)

---

### @kriskowal (MEMBER) — 2023-01-07T23:41:32Z

cc @michaelfig, reductive pubsub!

---

### @kriskowal (MEMBER) — 2023-01-11T20:21:35Z

Very interesting observation about `subscribeLatest` and `subscribeEach`. The use case this factors out of definitely needs to follow the tail, so suggests we need another verb like `subscribeEachUpdate` (lossless). I don’t think `subscribeLatest` falls out of this kind of pubsub. Or rather, our experience with Casting suggests that `subscribeLatest` is best implemented as a lazy async iterator, rather than an eager async iterator that gathers but discards intermediate values. However, a pressure relief adapter on an async reader is definitely possible:.

---

### @kriskowal (MEMBER) — 2023-01-15T07:45:32Z

This change now includes three flavors of pubsub, and documentation for what they do and what they’re suited for, and how they optimize transit. I’ve also added an independently reviewable commit that adds `Far` wrappers to all remoteable objects. To that end, it was necessary to rewrite `mapWriter` in terms of the iterator protocol since we can’t use `Far` on async iterators produced by async generator functions (because they have a funny prototype chain).

---

### @michaelfig (MEMBER) — 2023-01-15T21:45:44Z

> we can’t use `Far` on async iterators produced by async generator functions (because they have a funny prototype chain).

I've used the following approach to paper over problems like this, if I understand what you're saying.

```js
const makeNearAsyncIterable = aitP => Far('AsyncIterable', {
  [Symbol.asyncIterator]: () => {
    const ait = E(aitP)[Symbol.asyncIterator]();
    return Far('AsyncIterator', {
      next: (...args) => E(ait).next(...args),
    };
  },
});
const ait = makeNearAsyncIterable(asyncGenerator());
```


---

### @kriskowal (MEMBER) — 2023-01-21T00:20:42Z

I’m holding this change off `master` until after @erights moves the core of Agoric SDK pubsub up to Endo so I can evaluate whether this layering fits within the bigger picture.

---

### @kriskowal (MEMBER) — 2023-06-07T00:29:25Z

I’ve trimmed this PR down to just an internal implementation of makeChangeTopic that we can substitute for a notifier without changing any public interfaces. My intention is to get this on master before notifier’s have moved. Please consider whether you’re on board with this direction @mfig @erights.

---

### @kriskowal (MEMBER) — 2023-08-17T23:54:49Z

Plan remains to substitute this pubsub with the light-client approach from Agoric-SDK. I’m fully convinced that this approach, while interesting, isn’t adequate. The Pet Daemon spike in `endo` has is tall with weeds so closing all these out with an expectation that I’ll have to retcon the spike.

