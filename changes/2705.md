# #2705: feat(compartment-mapper): Make bundle using named evaluator

- **URL:** https://github.com/endojs/endo/pull/2705
- **State:** CLOSED
- **Author:** @kriskowal
- **Created:** 2025-01-27T23:50:41Z
- **Updated:** 2025-01-31T00:09:44Z
- **Closed:** 2025-01-31T00:09:43Z
- **Merged:** <no value>
- **Base:** master
- **Head:** kriskowal-named-evaluate
- **Draft:** false
- **Files:** 8 (+161 −25)
- **Labels:** 

## Summary

- **What:** Adds a `useNamedEvaluate` option to `@endo/compartment-mapper`'s `makeBundle` so each module functor in the `nestedEvaluate` bundle format calls a named evaluator instead of evaluating inline.
- **Why:** Step toward replacing Rollup behind `getExport`/`nestedEvaluate` so `bundle-source` can preserve formatting and source maps through censorship-evasion transforms (#2444); subsumed into #2707.
- **Themes:** compartment-mapper, bundle-source
- **Status:** closed-unmerged

## Description

Refs: #2444 

## Description

Toward better preservation of formatting through the ModuleSource and censorship evasion transforms (#2444), we ran into difficulty in composition with Rollup and source map transformations. The simplest route forward is to replace the Rollup implementation behind `getExport` and `nestedEvaluate` bundle formats with Endo’s own bundler. To that end, the `nestedEvaluate` format needs to be able to evaluate each module functor with the _named evaluate_ function instead of evaluating them inline. This change introduces a `useNamedEvaluate` option to `makeBundle`, which facilitates the `nestedEvaluate` bundle format.

### Security Considerations

The `nestedEvaluate` bundle format runtime in `importBundle` is obliged to provide a confined evaluator and also confine the evaluation of the bundle proper, as the intent is to confine the bundle.

### Scaling Considerations

None.

### Documentation Considerations

In a subsequent change, where we carve out the implementation of `nestedEvaluate`, there must be notes about the change in behavior this implementation will cause over the prior Rollup.

### Testing Considerations

This change includes a pair of tests to cover the bundler behavior and also to verify that the resulting evaluation preserves line numbers in error stacks.

### Compatibility Considerations

This is purely additive at this layer, but does not perfectly emulate the behavior of Rollup.

### Upgrade Considerations

When this change becomes incorporated in `bundleSource`, compensating changes are required in the contemporary versions of Agoric SDK kernel components. These changes have already landed. https://github.com/Agoric/agoric-sdk/pull/10878 https://github.com/Agoric/agoric-sdk/pull/10877

## Commits (1)

- `76c8489594f8d46bb6d18af1abd5db986590855d` feat(compartment-mapper): Make bundle using named evaluator

## Reviews (2)

---

### @gibson042 — APPROVED — 2025-01-28T21:37:10Z

This works, although I find myself wondering if `${useNamedEvaluate}(${JSON.stringify(cjsFunctor)})` is _too_ general. Are there anticipated use cases for something other than recursive evaluation?

---

### @kriskowal — COMMENTED — 2025-01-28T23:44:56Z




## Comments (4)

---

### @kriskowal (MEMBER) — 2025-01-28T00:33:37Z

attn @naugtur since you have a hand in the maintenance of the compartment-mapper/bundle.js feature.

---

### @naugtur (MEMBER) — 2025-01-28T18:59:10Z

I can't say I understand what nestedEvaluate is for. Gonna need some time to look into it.

---

### @kriskowal (MEMBER) — 2025-01-29T00:48:52Z

These are good questions.

> This works, although I find myself wondering if `${useNamedEvaluate}(${JSON.stringify(cjsFunctor)})` is _too_ general. Are there anticipated use cases for something other than recursive evaluation?

> Assuming this is the expected use pattern, are there concerns about the endowment being retrieved and possibly exploited by source code? Although the options for addressing that are limited and messy.

It might be too general. Expected values would be `"eval"` or _like_ `"evaluate"` if you use a compartment and endow it with an `evaluate` function. We aren’t constraining the name of the evaluator here, and maybe could and should. In subsequent changes, I might generalize `makeBundle` so it can curry an options bag instead of grabbing the named evaluate out of lexical scope. But, for the SES dist bundles, it’s important that the completion value of the the bundle is discardable. For `nestedEvaluate`, on the other hand, it’s useful to be able to inject a `sourceUrlPrefix` at runtime (coming soon). It might be that we can limit `useNamedEvaluate` to the case where the options bag gets curried, and then the name becomes irrelevant: it’s injected.

I have your stamp, but I will hold this open and stack subsequent, non-orthogonal changes so we can revisit.

---

### @kriskowal (MEMBER) — 2025-01-31T00:09:43Z

Subsumed into https://github.com/endojs/endo/pull/2707

