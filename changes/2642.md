# #2642: feat(compartment-mapper): Relax package discovery

- **URL:** https://github.com/endojs/endo/pull/2642
- **State:** MERGED
- **Author:** @kriskowal
- **Created:** 2024-11-21T01:29:42Z
- **Updated:** 2024-11-26T00:41:23Z
- **Closed:** 2024-11-26T00:41:22Z
- **Merged:** 2024-11-26T00:41:22Z
- **Base:** master
- **Head:** kriskowal-relax-discovery-2636
- **Draft:** false
- **Files:** 10 (+124 −8)
- **Labels:** 

## Summary

- **What:** Make `@endo/compartment-mapper` tolerate physically absent `peerDependencies`/`optionalDependencies` by default and add a `strict` option that restores the prior loud error during `mapNodeModules`.
- **Why:** The strict interpretation is technically correct but unevenly observed across the ecosystem; deferring the error to `link/load` is friendlier (closes #2636).
- **Themes:** compartment-mapper, ecosystem-compat, errors
- **Status:** merged

## Description

Closes: #2636 

## Description

The compartment mapper is currently strict about the interpretation of `peerDependencies` and `peerDependenciesMeta.optional` as opposed to `optionalDependencies` and is technically correct, but that correctness and understanding is not evenly distributed among tools in the ecosystem.
If the Compartment Mapper instead tolerates the physical absence of expected packages, it defers an error that would be experienced under `mapNodeModules` to `link/load` since that might attempt to load a module from the missing package. This is not a particularly observable difference in behavior, but the error message will be slightly less informative.

In this change, we relax the behavior by default and provide a `strict` option to provide the more informative error and tolerate fewer ecosystem defficiencies.

### Security Considerations

None.

### Scaling Considerations

None.

### Documentation Considerations

News updated.

We do not yet publish API documentation for Compartment Mapper, but the necessary annotations are present when these devices are more public.

### Testing Considerations

Includes positive and negative tests for the `strict` behavior.

### Compatibility Considerations

The nature of some errors during bundling may change. Bundling errors are not observed on Agoric’s chain, so that sensitivity should not cause problems.

### Upgrade Considerations

None.

## Commits (1)

- `ab885a226eb05eb715df9da05c63c11731abf494` feat(compartment-mapper): Relax package discovery

## Reviews (3)

---

### @FUDCo — APPROVED — 2024-11-22T23:58:44Z

Gosh, this seems swell.

---

### @boneskull — COMMENTED — 2024-11-25T20:19:49Z

Does this have any effect on packages that are optionally `require`d but not otherwise mentioned in `package.json`?

An example from `typescript`:

```js
try {
  require('source-map-support')
} catch {}
```

...where `source-map-support` is a _dev_ dependency of `typescript`.  So this `require` would _not_ throw if a) someone has a local working copy of `Microsoft/typescript` or b) someone already has `source-map-support` in their `node_modules` (as they often do).


---

### @kriskowal — COMMENTED — 2024-11-26T00:24:52Z




## Comments (2)

---

### @kriskowal (MEMBER) — 2024-11-26T00:26:28Z

> Does this have any effect on packages that are optionally `require`d but not otherwise mentioned in `package.json`?
> 
> An example from `typescript`:
> 
> ```js
> try {
>   require('source-map-support')
> } catch {}
> ```
> 
> ...where `source-map-support` is a _dev_ dependency of `typescript`. So this `require` would _not_ throw if a) someone has a local working copy of `Microsoft/typescript` or b) someone already has `source-map-support` in their `node_modules` (as they often do).

No, this just tolerates the absence in `.../node_modules` of packages that _are_ present in `package.json`, not accommodate the presence in `.../node_modules` of packages that are absent in `package.json`

---

### @kriskowal (MEMBER) — 2024-11-26T00:33:54Z

@boneskull We could conceivably add an even sloppier `mapNodeModules` that recursively lists `.../node_modules/{,@/}*` and disregards the `*dependencies` in `package.json` entirely. With a `link` hook, we might even be able to make discovery of `.../node_modules` lazy. In any case, not in scope for this PR, but so you know: that is a possibility.

