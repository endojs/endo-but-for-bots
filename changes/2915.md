# #2915: feat(compartment-mapper): add "preload" option to captureFromMap()

- **URL:** https://github.com/endojs/endo/pull/2915
- **State:** MERGED
- **Author:** @boneskull
- **Created:** 2025-07-31T22:21:45Z
- **Updated:** 2025-11-24T22:33:14Z
- **Closed:** 2025-11-24T22:33:11Z
- **Merged:** 2025-11-24T22:33:11Z
- **Base:** master
- **Head:** boneskull/force-load
- **Draft:** false
- **Files:** 10 (+356 −22)
- **Labels:** `enhancement`, `lavamoat`, `ecosystem-compatibility`

## Summary

- **What:** Adds a `preload` option to `@endo/compartment-mapper`'s `captureFromMap()`, accepting either compartment names or `{compartment, entry}` records, that loads named non-entry compartments after the entry/attenuator compartments to capture modules that would otherwise be missed by digestion (e.g. dynamic requires/imports).
- **Themes:** compartment-mapper, ecosystem-compat
- **Status:** merged

## Description

Prior art:

Ref: #2856
Ref: #2864
Ref: #2876

## Description

This adds option `preload` to `captureFromMap()`. This is an optional `string[]` of keys of the `CompartmentMapDescriptor` also provided to `captureFromMap()`. **It can also be an `Array<{compartment: string, entry: string}>` to preload a specific module other than the entry of that compartment.**

After loading the entry `Compartment` (and attenuators `Compartment`, if applicable), the `Compartment`s having names (practically speaking, these will be _locations_) mentioned in `preload` will then be loaded _if they were not already_.

This option can be used to support dynamic requires and imports which would otherwise be omitted from the captured `CompartmentMapDescriptor` via digestion.

> - Refactored `capture-lite.js`; stuffed all of the `Compartment`-loading business into a function.
> - Added test and fixture that shows how a `Compartment` which would otherwise be omitted from the captured Compartment Map is included when used with `preload`.

### Alternatives Considered

Besides the above references, I considered adding a `boolean` field `load` to `CompartmentDescriptor`. But I don't see how `@endo/compartment-mapper` would want to set this flag for any reason other than in `captureFromMap()` on the entry and attenuators compartments.  Thus, it seemed specific to `captureFromMap()` and so shouldn't be in `CompartmentDescriptor`.

Supplying an array of file URL strings is kind of a pain for the end-user, but they're right there in the `CompartmentMapDescriptor`, so it seems workable.

Open to naming suggestions.

### Security Considerations

Just the usual disclaimers about being careful what you put in the compartment map.

### Scaling Considerations

There's a tradeoff: use of `preload` has performance implications, since it causes (potentially _many_) module sources to be read and analyzed which would not otherwise be.

### Testing Considerations

There are probably some conditionals I've left uncovered.

### Compatibility Considerations

Backwards-compatible

### Upgrade Considerations

This is a user-facing change which should be described in `NEWS.md`.


## Commits (2)

- `45fb8c6dcf8bc8bd0a1f480602d2ebf893a4d469` chore(compartment-mapper): fix missing type reference
- `c3f5163b45982b56b88fab7779996bff8b9ed7ab` feat(compartment-mapper): allow force-loading of compartments

## Reviews (11)

---

### @boneskull — COMMENTED — 2025-08-04T20:41:15Z



---

### @boneskull — COMMENTED — 2025-08-04T20:45:43Z



---

### @boneskull — COMMENTED — 2025-08-04T20:53:57Z



---

### @boneskull — COMMENTED — 2025-08-04T21:31:12Z



---

### @kriskowal — COMMENTED — 2025-09-17T23:13:51Z

I’m sold on the need for the feature and mostly sold on the implementation.

I think `forceLoad` should be framed as a set of `{compartment, module}` duples, where `module` may default to `"."`.

I was confused during my review by the verb “load” being applied to compartments, since we capture compartments and load _modules_. This is indeed loading modules, as synecdoche for the main module of a compartment. That could be clearer in choices of names.

Consider some more names for `forceLoad`:
* `preload` (I think “force” is dissatisfying because we’re not overriding a safety measure.)
* `entries` (If this is an array of additional entry modules with the same type as the `entry` in `compartment-map.json`)

Consider, in fact, instead of a option, a way to add an `entries` field to the compartment map input to the argument. That might be threaded from an option higher up.

I had envisioned a feature of extra `entries` in `compartment-map.json` such that `app.import(name)` would produce one of the extra modules. This would be useful for staged evaluation of modules in a bundle.

```js
{
  "entry": { "module": ".", "compartment": "x" },
  "entries": {
    "y": { "module": ".", "compartment": "y"}
  }
}
```

---

### @boneskull — COMMENTED — 2025-10-21T20:13:57Z



---

### @naugtur — COMMENTED — 2025-10-22T13:00:29Z



---

### @naugtur — COMMENTED — 2025-10-22T13:02:27Z



---

### @naugtur — COMMENTED — 2025-10-22T13:07:13Z



---

### @boneskull — COMMENTED — 2025-11-17T21:00:04Z



---

### @naugtur — APPROVED — 2025-11-20T21:28:25Z




## Comments (4)

---

### @naugtur (MEMBER) — 2025-08-26T12:55:12Z

With outside-facing compartment map transforms the use of a marker in compartment map indicating what to load looks appealing again.
Instead of loading entry by default, the approach would be to load everything marked as a linkingStartNode, which would be true for entry by default. 

As a result adding missing links and linking start nodes based on policy overrides from lavamoat could happen in one transform provided by lavamoat.

I like the idea that compartment-mapper's linking and loading would be an engine to do whatever compartment map says, with as little configurability as possible. Configurability made more sense before transforms, but if we land transforms they could replace a lot of it.

---

### @boneskull (MEMBER) — 2025-08-27T01:16:26Z

@naugtur Yes, I may have mentioned above that I considered a similar design. I'm happy to go that way if we feel it's a better fit.  Still, there will need to be an option _somewhere_ because how else is `mapNodeModules` going to know which `CompartmentDescriptor` to flag for loading?

---

### @naugtur (MEMBER) — 2025-11-20T21:28:15Z

> @naugtur Yes, I may have mentioned above that I considered a similar design. I'm happy to go that way if we feel it's a better fit. Still, there will need to be an option _somewhere_ because how else is `mapNodeModules` going to know which `CompartmentDescriptor` to flag for loading?

When we introduce hooks, the packageDependenciesHook could be where we add things and mark them to be preloaded. The first call of the hook could have a falsy value for canonicalName to indicate the dependencies returned are entries. Or, if it's too magical, the hook could be allowed to return multiple items and if some of the items have previously unknown canonical names, we add them to compartment map as entries to be preloaded.

i'm not sure if the ideas above are better than what we have here, but I do want to leave room for exploring doing preload via hooks making changes to data.

Happy to merge this on condition that the preload option is marked as unstable and to be changed, so that we can use it in lavamoat and leave room for finding out a nicer way to do it.

Approving on condition that it's documented as unstable/experimental

---

### @boneskull (MEMBER) — 2025-11-24T22:23:27Z

OK.  I'm going to rename the option `_preload` and just omit it from `NEWS.md` until it becomes non-experimental or we refactor it out.

