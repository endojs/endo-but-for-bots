# #1594: fix(marshal): Add a smaller and more efficient passable encoding

- **URL:** https://github.com/endojs/endo/pull/1594
- **State:** MERGED
- **Author:** @gibson042
- **Created:** 2023-05-23T20:24:10Z
- **Updated:** 2024-02-05T17:06:05Z
- **Closed:** 2024-02-05T17:06:04Z
- **Merged:** 2024-02-05T17:06:04Z
- **Base:** master
- **Head:** gibson-1349-encodepassable-escaping
- **Draft:** false
- **Files:** 4 (+687 −91)
- **Labels:** 

## Summary

- **What:** Adds a new "compactOrdered" passable encoding to `@endo/marshal`'s `encodePassable`, exposes `makePassableKit` for arbitrary remotable/promise/error encodings, escapes C0 controls, and tightens character handling.
- **Why:** Fixes #1349; previous "legacyOrdered" encoding was bulky and order-fragile when embedded in JSON capData.
- **Themes:** marshal, performance, ocapn
- **Status:** merged

## Description

Fixes #1349

Open questions:
- [x] [**REJECTED**] Should we push for extra compactness, e.g. `bfalse` → `bf`/`btrue` → `bt` (booleans) and/or `p1:0` → `o` (0n)?
- [x] [**REJECTED**] Should we allow custom remotable/promise/error encoding to use sequences and/or characters that would not otherwise be allowed, such as raw control characters U+0000 through U+001F or raw string escape characters `!`/`_`?
- [x] [**REPLACE ALL C0 CONTROLS**] For that matter, should we replace the full U+0000 through U+001F range in strings, or just a subset? The full range is necessary if U+0020 SPACE is the array element separator, but that could alternatively be U+0009 TAB and escapes could be limited to code points at or before that. This is really a tradeoff around expectations of character frequency inside strings, limiting blowup when embedding encodings in JSON-serialized containers such as current CapData communication, and (to some extent) human comprehensibility. There's not really a performance difference and at most a linear compactness difference, so it's really a question of aesthetics—for example, JSON-serializing the encoding of `{ "metasyntactic variables": ["foo", "bar", "baz"], tab: "\t", null: "\0" }` would change as follows if we used tab-termination rather than space-termination:
  ```diff
  -"~(^^stab snull smetasyntactic!_variables  ^s!* s!! ^sfoo sbar sbaz   "
  +"~(^^stab\tsnull\tsmetasyntactic variables\t\t^s\n9\ts\n0\t^sfoo\tsbar\tsbaz\t\t\t"
  ```

## Commits (14)

- `a85616fdf4c1cf4104ef4921cdade4fb040e4068` fix(marshal): Add a smaller and more efficient passable encoding
- `d90dfe2f4c7ba89f7b7e9b91087e63838cf7a11e` test(marshal): Make a macro for encodePassable round-trip tests
- `961c7c38a10f0c29c1e02d3e62989336159c5b23` refactor(marshal): Use String#replace rather than replaceAll
- `52dbda60877b38b490aca340599fcd57da27cf54` chore(marshal): Improve comments
- `242d90599b7a619a869555042f3f6a5660b2c6d8` refactor(marshal): Change new-encoding prefix symbol from "#" to "~"
- `4ed2c15c3987b6395521f3ec433f57dad0e95387` chore(marshal): Consistently assert behavior of makeEncodePassable re…
- `820932a886036348c8da85afe775c644b82d401d` chore(marshal): Name the encodePassable formats ("legacyOrdered" and …
- `1486557a29f0fb0feb704345b4c8a105b01d27c9` chore(marshal): Escape remotable/promise/error encodings in compactOr…
- `043039f273802cc74806de71294425fc01384a9d` feat(marshal): Introduce makePassableKit and support arbitrary remota…
- `0afce72d0f28782da3618ad6d4b6821cfee41e9b` chore(marshal): Reject strings containing characters that *should* be…
- `d6abd2cbcf7a4b8db988406429bff99155342636` chore(marshal): Disallow C0 control characters in custom encodings
- `52a67f9763f97d8657de35888d5e570939e7a853` fixup! feat(marshal): Introduce makePassableKit and support arbitrary…
- `59b02aae5a87050819298bab77621cf4c7d39afa` perf(marshal): Avoid even more substring operations
- `70bcca3d4ba93e92221a9188f583126ca84e4e4d` docs(marshal): Start documenting passable encoding

## Reviews (37)

---

### @gibson042 — COMMENTED — 2023-05-23T20:48:19Z



---

### @kriskowal — COMMENTED — 2023-06-23T19:08:54Z



---

### @kriskowal — COMMENTED — 2023-06-23T19:11:40Z



---

### @kriskowal — COMMENTED — 2023-06-23T19:12:26Z



---

### @erights — CHANGES_REQUESTED — 2023-08-07T01:28:07Z

Sorry this took so long!

Looking forward to using this! I think I can start making use of this soon.

---

### @gibson042 — COMMENTED — 2023-08-26T14:59:47Z



---

### @gibson042 — COMMENTED — 2023-08-26T16:15:01Z



---

### @gibson042 — COMMENTED — 2023-08-26T16:38:14Z



---

### @gibson042 — COMMENTED — 2023-08-26T16:40:41Z



---

### @gibson042 — COMMENTED — 2023-08-26T16:56:55Z



---

### @gibson042 — COMMENTED — 2023-08-26T16:59:44Z



---

### @erights — COMMENTED — 2023-08-27T03:25:42Z



---

### @erights — COMMENTED — 2023-08-27T03:28:26Z



---

### @erights — COMMENTED — 2023-08-27T03:30:05Z



---

### @erights — COMMENTED — 2023-08-27T03:34:38Z



---

### @erights — COMMENTED — 2023-08-27T03:35:31Z



---

### @erights — COMMENTED — 2023-08-27T03:38:54Z



---

### @erights — COMMENTED — 2023-08-27T03:41:00Z



---

### @erights — COMMENTED — 2023-08-28T01:35:13Z



---

### @erights — COMMENTED — 2023-08-28T01:36:12Z



---

### @erights — COMMENTED — 2023-08-28T01:36:28Z



---

### @gibson042 — COMMENTED — 2023-08-28T20:33:11Z



---

### @gibson042 — COMMENTED — 2023-08-28T20:43:28Z



---

### @erights — COMMENTED — 2023-08-28T21:15:35Z



---

### @erights — COMMENTED — 2023-08-28T21:16:18Z



---

### @gibson042 — COMMENTED — 2023-08-28T21:16:47Z



---

### @erights — COMMENTED — 2023-08-28T21:19:47Z



---

### @gibson042 — COMMENTED — 2023-08-28T21:22:01Z



---

### @erights — COMMENTED — 2023-08-28T21:24:19Z



---

### @gibson042 — COMMENTED — 2023-08-29T18:26:39Z



---

### @gibson042 — COMMENTED — 2023-08-29T19:13:02Z



---

### @erights — COMMENTED — 2023-08-29T22:34:22Z



---

### @erights — APPROVED — 2023-09-11T21:59:27Z

LGTM, thanks!

(If possible, would be nice to see this merged before tomorrow's ocapn mtg)

---

### @gibson042 — COMMENTED — 2023-09-12T22:43:27Z



---

### @gibson042 — COMMENTED — 2023-09-13T12:11:31Z



---

### @erights — APPROVED — 2024-01-10T00:21:59Z

I am happy with all these decisions. Please proceed, thanks!


---

### @warner — COMMENTED — 2024-01-17T19:11:15Z




## Comments (16)

---

### @erights (CONTRIBUTOR) — 2023-06-23T03:54:27Z

Hopefully fixes https://github.com/Agoric/agoric-sdk/issues/2589 , yes?

---

### @erights (CONTRIBUTOR) — 2023-08-18T02:42:22Z

Btw @gibson042 , I reviewed it as if it is Ready For Review. Is there a reason it is still Draft?

---

### @erights (CONTRIBUTOR) — 2023-08-18T02:42:48Z

Any idea why CI didn't complete?

---

### @gibson042 (MEMBER) — 2023-08-26T17:07:31Z

> Hopefully fixes [Agoric/agoric-sdk#2589](https://github.com/Agoric/agoric-sdk/issues/2589) , yes?

Yes, I think we can make that claim.

> Btw @gibson042 , I reviewed it as if it is Ready For Review. Is there a reason it is still Draft?

I consider it a draft until we settle on a name.

> Any idea why CI didn't complete?

Pushed to kick CI, and I can also rebase on master if that won't disrupt review.

---

### @erights (CONTRIBUTOR) — 2023-08-26T23:42:32Z

> and I can also rebase on master if that won't disrupt review

Please do rebase to master, thanks.

---

### @gibson042 (MEMBER) — 2023-09-07T21:23:55Z

Updated to use "legacyOrdered" and "compactOrdered" names—so this PR is no longer a draft!

---

### @gibson042 (MEMBER) — 2023-09-12T20:05:59Z

Returning to draft because I found that the new format needs more handling for output from functions provided to `makeEncodePassable`:
```js
import "@endo/init";
import { Far } from "@endo/far";
import { makeMarshal, makeEncodePassable } from "@endo/marshal";
const r0 = Far("Foo");
const data = harden([r0, r0, "."]);
const safeEncodeRemotable = r => "r0";
const unsafeEncodeRemotable = r => "r 0";
const baseOptions = { format: "compactOrdered" };
console.log({
  safeEncoding: makeEncodePassable({ ...baseOptions, encodeRemotable: safeEncodeRemotable })(data),
  unsafeEncoding: makeEncodePassable({ ...baseOptions, encodeRemotable: unsafeEncodeRemotable })(data),
});
/* =>
{
  safeEncoding: '~^r0 r0 s. ',
  unsafeEncoding: '~^r 0 r 0 s. '
}
*/
```

---

### @gibson042 (MEMBER) — 2023-09-12T22:42:01Z

> Returning to draft because I found that the new format needs more handling for output from functions provided to `makeEncodePassable`

Resolved now; @erights please review the updates at https://github.com/endojs/endo/pull/1594/commits/9c9d1e3baf20fdb667156bf6d98c43d68433c6c4 .

---

### @erights (CONTRIBUTOR) — 2023-09-13T21:01:36Z

Could we provide it such a decode function?

On Wed, Sep 13, 2023 at 5:11 AM Richard Gibson ***@***.***>
wrote:

> ***@***.**** commented on this pull request.
> ------------------------------
>
> In packages/marshal/src/encodePassable.js
> <https://github.com/endojs/endo/pull/1594#discussion_r1324416864>:
>
> > +  } else if (format === 'compactOrdered') {
> +    formatPrefix = '~';
> +    encodeStringSuffix = encodeCompactStringSuffix;
> +    encodeArray = encodeCompactArray;
> +  } else {
> +    Fail`Unrecognized format: ${q(format)}`;
> +  }
> +  const encodeRemotable = makeEncodeRemotable(
> +    unsafeEncodeRemotable,
> +    encodeStringSuffix,
> +  );
> +  const encodePromise = makeEncodePromise(
> +    unsafeEncodePromise,
> +    encodeStringSuffix,
> +  );
> +  const encodeError = makeEncodeError(unsafeEncodeError, encodeStringSuffix);
>
> This double-escapes the contents of any embedded string (such as for the
> error message), but I don't see a good alternative. In particular,
> asserting that unsafeEncodeError produced an independently valid encoding
> that needs no further escaping would be awkward because this code doesn't
> have access to any caller-provided decode functions for embedded
> remotables/promises/errors.
>
> —
> Reply to this email directly, view it on GitHub
> <https://github.com/endojs/endo/pull/1594#pullrequestreview-1624387431>,
> or unsubscribe
> <https://github.com/notifications/unsubscribe-auth/AACC3TAZEZWGDV3GPWD3SW3X2GPH7ANCNFSM6AAAAAAYMMXCXA>
> .
> You are receiving this because you were mentioned.Message ID:
> ***@***.***>
>


---

### @gibson042 (MEMBER) — 2023-09-14T01:07:26Z

Yes, with a refactoring to be more like `makeMarshal`.

---

### @erights (CONTRIBUTOR) — 2023-09-18T06:12:54Z

> Yes, with a refactoring to be more like `makeMarshal`.

This seems attractive. Is there any reason not to?

---

### @erights (CONTRIBUTOR) — 2023-12-01T18:49:00Z

ping?

---

### @gibson042 (MEMBER) — 2024-01-09T08:34:52Z

@erights I've avoided the double-escaping by instead requiring caller-provided encode{Remotable,Promise,Error} functions to return valid encodings, which is trivial for the legacy format (because escaping is not applied until a value is wrapped in an array) but not for the compact format, and introduced `makePassableKit` to hold the shared encode/decode functionality for such verification (reimplementing `makeEncodePassable` and `makeDecodePassable` to rely upon it). This means a heavier reliance upon the callback for encoding values with substructure (such as errors), but on balance seems like an improvement to me.

I've also documented the remaining interesting questions above:
> - Should we push for extra compactness, e.g. `bfalse` → `bf`/`btrue` → `bt` (booleans) and/or `p1:0` → `o` (0n)?
> - Should we allow custom remotable/promise/error encoding to use sequences and/or characters that would not otherwise be allowed, such as raw control characters U+0000 through U+001F or raw string escape characters `!`/`_`?
> - For that matter, should we replace the full U+0000 through U+001F range in strings, or just a subset? The full range is necessary if U+0020 SPACE is the array element separator, but that could alternatively be U+0009 TAB and escapes could be limited to code points at or before that. This is really a tradeoff around expectations…

---

### @gibson042 (MEMBER) — 2024-01-09T16:33:55Z

My take:
* Extra compactness of booleans and 0n probably doesn't matter, so basically "don't care" and status quo would stand by default.
* To what extent custom remotable/promise/error encodings should be allowed to color outside the lines is interesting. I consider the `!`- and `_`-prefixed escapes to be details of the built-in string encoding and therefore not mandatory (although convenient to adopt), but would like to reject any use of U+0000 through U+001F (_U+0020 SPACE and 	U+005E CIRCUMFLEX ACCENT `^` are already effectively disallowed by inescapable interpretation as array syntax_)... there's just too much risk of being a vector to produce e.g. misleading console output to justify the small benefit of naïveté in encode functions.
* I predict that element terminators will be more common than space characters inside strings, and also that use of JSON will persist (among other reasons, for its performance benefits). Therefore I like the use of U+0020 SPACE as the element terminator, despite the loss of readability inside encoded strings (replacing spaces with `!_`) and the loss of compactness in a non-JSON binary container or with especially space-heavy strings. That is, I _don't_ think we should pursue this kind of change:
  ```diff
  -"~(^^stab snull smetasyntactic!_variables  ^s!* s!! ^sfoo sbar sbaz   "
  +"~(^^stab\tsnull\tsmetasyntactic variables\t\t^s\n9\ts\n0\t^sfoo\tsbar\tsbaz\t\t\t"
  ```

---

### @warner (CONTRIBUTOR) — 2024-01-17T19:21:04Z

Any chance we could get a neighboring `.md` document that explains the format?

This document would also be a good place to mention that the `format:` option limits the decoder's expectations.. if I read this correctly, there's no option to build a decoder that can handle both new- and old- format data. To use this in liveslots, we'll either need:

* 1: Set a vat-wide flag to say what format we're using for all virtual collection keys. If we're launching a brand new vat, we can set it to use the new format forever. If the new-format-capable liveslots is loaded in a pre-existing vat (ie vat upgrade), and it doesn't see this flag, it must stick with the old format forever.
* or 2: Introduce some sort of prefix in the vatstore records to indicate whether each record is new- or old- format. The current vatstore key is like `vc.5.sfoo` (where `vc` means "virtual collection", `5` means collection number 5, and `sfoo` is the passable-encoded string key `foo`). If there's a format key (`s/n/p/b/r`/etc) that's guaranteed to not appear in the old format, we could use that as a per-record indicator, but it would break sort order between old- and new- format keys of the same category.  So that doesn't leave us much/any room for a per-record format indicator, but maybe we could do a per-collection indicator, and at least allow collections created after the upgrade to use the new format.


---

### @mhofman (CONTRIBUTOR) — 2024-01-17T20:19:47Z

> * Introduce some sort of prefix in the vatstore records to indicate whether each record is new- or old- format.

I expect this to be per collection to preserve sort order between entries, so it could be in the vc metadata.

