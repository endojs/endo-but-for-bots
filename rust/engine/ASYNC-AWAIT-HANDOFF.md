# Async/await documentation moved

The invocation-specific handoff has been retired.
Current ownership, GC roots and the XS mechanism map are in
[Architecture: async and generator roots](ARCHITECTURE.md#async-and-generator-roots).
This small redirect preserves links in the verbatim historical changelog.
Use the executable GC registry and carry tests linked there for current contracts.
