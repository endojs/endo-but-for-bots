# Issue 118: OCapN Noise Integration in Daemon

- **Contributor:** [`kriskowal`](https://github.com/kriskowal) (maintainer)
- **State at last observation:** OPEN
- **Snapshot timestamp:** 2026-05-07 07:30 UTC

## Posture

Two-part request, both targeting the `llm` branch:

1. Consolidate the three stacked OCapN Noise PRs against
   `master` (#111 CBOR codec + NonceLocator, #112 Noise IK
   netlayer, #113 integration + transport tests) into a single
   PR targeting `llm`, so the maintainer can begin validating
   the netlayer + CBOR code on that branch.
2. Author a design document (also for landing on `llm`) that
   proposes how to integrate OCapN with the Daemon, as a
   replacement or alternative to Endo CapTP. Likely entails
   reenvisioning the existing `@nets` design as `@transports`,
   where transports are per-agent (guest or host), can come and
   go, and may differ across agents.

Both items are code/design work the liaison cannot author.
Surfaced to the steward for builder/weaver (consolidation PR)
and researcher/designer (design doc) dispatch in upcoming
cycles. Reply on the issue acknowledges receipt, names the
three source PRs, and classifies the two deliverables.

## Response log

- **2026-05-07 07:30 UTC** — observed: issue opened by maintainer
  requesting (a) a consolidation PR against `llm` for stacked
  PRs #111/#112/#113 and (b) a design doc for OCapN/Daemon
  integration with `@transports` reenvisioning of `@nets`;
  response: posted `eyes` reactji on issue body
  (<https://github.com/endojs/endo-but-for-bots/issues/118>),
  posted reply listing the three source PRs and classifying the
  two surface-to-steward work items
  (<https://github.com/endojs/endo-but-for-bots/issues/118#issuecomment-4395214220>).

## Outstanding

- Steward dispatch pending: builder/weaver subagent to produce
  a single consolidation PR against the `llm` branch combining
  #111, #112, #113. Source PRs are stacked, so the consolidation
  is a rebase-and-flatten onto `llm`.
- Steward dispatch pending: researcher/designer subagent to
  draft the OCapN/Daemon integration design doc on `llm`,
  proposing the `@transports` model (per-agent, ephemeral) as
  the successor to `@nets`. Deliverable is the design doc; the
  implementation follows in a separate cycle.
