# #2656: Testing should assert the expected name of console methods

- **URL:** https://github.com/endojs/endo/issues/2656
- **State:** OPEN
- **Author:** @gibson042
- **Created:** 2024-12-06T19:57:05Z
- **Updated:** 2025-08-28T00:28:26Z
- **Labels:** `enhancement`, `debugging support`

## Description

## What is the Problem Being Solved?

As noted in https://github.com/endojs/endo/pull/2654#pullrequestreview-2483353718 , it would be nice to cover the expected name of [virtual] console methods.

## Description of the Design
e.g.,
```js
const keysToCheck = new Set([
  ...consoleLevelMethods,
  ...consoleOtherMethods,
  ...Reflect.ownKeys(console),
]);
for (const key of keysToCheck) {
  const value = console[key];
  if (typeof value === 'function') {
    const expectedName = typeof key === 'symbol'
      ? `[${key.description}]`
      : key;
    t.is(value.name, expectedName, `console.${String(key)}.name`);
  }
}
```
for the console object returned by use of `makeLoggingConsoleKit`/`makeCausalConsole`/`filterConsole`/etc.

## Security Considerations
n/a

## Scaling Considerations
n/a

## Test Plan
Refer to the above design.

## Compatibility Considerations
n/a

## Upgrade Considerations
n/a

## Comments (0)

