# #2600: Should encodePassable format "compactOrdered" not use "~" as an encoding prefix?

- **URL:** https://github.com/endojs/endo/issues/2600
- **State:** OPEN
- **Author:** @gibson042
- **Created:** 2024-10-21T21:05:30Z
- **Updated:** 2024-10-21T21:05:30Z
- **Labels:** `bug`

## Description

## Describe the bug

As implied at https://github.com/endojs/endo/blob/b3a31b1559118dcfcd3811e9632c3f28fba99a3c/packages/marshal/src/encodePassable.js#L861-L865 and noted explicitly in https://github.com/Agoric/agoric-sdk/issues/2589#issuecomment-2427675183 , [agoric-sdk liveslots](https://github.com/Agoric/agoric-sdk/tree/master/packages/swingset-liveslots) expects `|` to be a prefix that sorts after all encoded values. But that assumption would no longer hold if `~`-prefixed compactOrdered encodings were stored as-is. Here in endo, we can either assume that agoric-sdk will find a suitable workaround, or switch to a different prefix that does not suffer from this issue. The [initial choice of `~`](https://github.com/endojs/endo/pull/1594#discussion_r1202975796) was mostly arbitrary, but did strive to avoid overloading any existing special character. Were we to stick with that goal, we'd be looking at candidates like
* U+0020 SPACE (which sorts before any other encoding but suffers from invisibility)
* `{` (which sorts after any other encoding but overloads the JSON begin-object token)
* `"` (which overloads the JSON string quote)
* `#` (which overloads the CapData body smallcaps prefix and smallcaps special-constant prefix)
* `$`/`%`/`&`/`+`/`-` (which overload smallcaps type prefixes)
* `'`/`)`/`*`/`,` (which are reserved by smallcaps but not currently used)
* `.`/`/`/`;`/`<`/`=`/`>`/`@`/`\`/`]`/`_`/``` ` ``` (no known conflicts other than sorting in between legacy passable encodings)

## Comments (0)

