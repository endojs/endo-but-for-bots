# #1706: expose `re-export` field in StaticModuleRecord

- **URL:** https://github.com/endojs/endo/issues/1706
- **State:** OPEN
- **Author:** @himself65
- **Created:** 2023-07-30T02:29:35Z
- **Updated:** 2024-01-08T19:40:17Z
- **Labels:** `kriskowal-review-2024-01`

## Description



## Comments (4)

---

### @kriskowal (MEMBER) — 2023-07-31T03:07:40Z

Can you tell me more about your use case?

In general, `StaticModuleRecord` needs some work to realign with standard proposals for a `ModuleSource` constructor. For that, we are likely to follow the lead of XS and implement a `bindings` array, where you would find `{ exportAllFrom: 'specifier.js' }` for the reëxports case.

---

### @himself65 (NONE) — 2023-08-03T17:13:08Z

Sorry for the late reply. I'm implementing some sandbox in the browser to safely run our plugin in our app in the browser.

My current implementation is to compile the code into some correct code that can be run in `eval` (Compartment.evaluate) **because you cannot use the `import` statement in the browser**

For example the compiled hello-world code:

```js
({ imports: $h‍_imports, liveVar: $h‍_live, onceVar: $h‍_once, importMeta: $h‍____meta }) => {
  let rootStore, currentWorkspaceIdAtom, lazy, createElement, createRoot;
  $h‍_imports([["@toeverything/plugin-infra/atom", [["rootStore", [($h‍_a) => rootStore = $h‍_a]], ["currentWorkspaceIdAtom", [($h‍_a) => currentWorkspaceIdAtom = $h‍_a]]]], ["react", [["lazy", [($h‍_a) => lazy = $h‍_a]], ["createElement", [($h‍_a) => createElement = $h‍_a]]]], ["react-dom/client", [["createRoot", [($h‍_a) => createRoot = $h‍_a]]]]]);
  const HeaderItem = lazy(
    () => $h‍_import("./app-cca8b2.mjs").then(({ HeaderItem: HeaderItem2 }) => ({ default: HeaderItem2 }))
  );
  const entry = (context) => {
    console.log("register");
    console.log("hello, world!");
    return () => {
      console.log("unregister");
    };
  };
  $h‍_once.entry(entry);
};
```

In this case, I need to know all exports, imports, and re-exports before I evaluate this code. So, I need to generate it along with the js code.

For example:

```json
{
  "exports": {},
  "imports": [
    "react",
    "react-dom/client",
    "./index-2d1f56.mjs",
    "react/jsx-runtime",
    "@affine/component",
    "jotai",
    "jotai/utils",
    "@blocksuite/icons",
    "@toeverything/plugin-infra/atom"
  ],
  "reexports": {
    "./index-2d1f56.mjs": [
      [
        "e",
        "entry"
      ]
    ]
  }
}
```

I just read your test cases and find that I can use `__reexportMap__`, but I'm not sure this is the good approach

https://github.com/toeverything/AFFiNE/blob/0e328032478239ec11741bf1c97ca368a4240c2d/packages/plugin-cli/src/af.ts#L177-L202


---

### @himself65 (NONE) — 2023-08-03T17:15:40Z

BTW, I implement an ESM-simulated environment in the browser. Not sure if you have a better replacement or a good idea

https://github.com/toeverything/AFFiNE/blob/0e328032478239ec11741bf1c97ca368a4240c2d/apps/core/src/bootstrap/plugins/setup.ts#L306-L388

---

### @himself65 (NONE) — 2023-08-03T17:23:45Z

And another is that using `StaticModuleRecord` is super slow in the browser. I think because Babel compiles the large code. That's also why I choose pre-compiled code

