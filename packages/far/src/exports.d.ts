/**
 * @deprecated Import these names (`FarRef`, `ERef`, `EOnly`, `EReturn`,
 *   `EResult`) directly from `@endo/eventual-send`, their originating package
 *   (see designs/inter-package-plain-re-exports.md).
 */
export { FarRef, ERef, EOnly, EReturn, EResult } from '@endo/eventual-send';
