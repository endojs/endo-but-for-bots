// The type re-exports that used to live here (`FarRef`, `ERef`, `EOnly`,
// `EReturn`, `EResult` from `@endo/eventual-send`) were plain re-exports and
// have been removed per endojs/endo-but-for-bots#543. Import those types
// directly from `@endo/eventual-send`.
export {};
