/**
 * @deprecated Import `E` directly from `@endo/eventual-send`, its originating
 *   package (see designs/inter-package-plain-re-exports.md).
 */
export { E } from '@endo/eventual-send';

/**
 * @deprecated Import these names (`Far`, `getInterfaceOf`, `passStyleOf`)
 *   directly from `@endo/pass-style`, their originating package (see
 *   designs/inter-package-plain-re-exports.md).
 */
export { Far, getInterfaceOf, passStyleOf } from '@endo/pass-style';

// eslint-disable-next-line import/export
export * from './exports.js';
