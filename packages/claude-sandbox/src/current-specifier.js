// @ts-check

/**
 * The release-pinned specifier rerouting every adapter's setup needs; see
 * `@endo/hosted-agent/current-specifier.js`.
 * @module
 */
export {
  assertCurrentSpecifier,
  toCurrentSpecifier,
} from '@endo/hosted-agent/current-specifier.js';
