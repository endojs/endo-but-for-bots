// transdep is type:module, so these `.js` modules would default to ECMAScript.
// They are reached by RELATIVE import within this transitive dependency; their
// language is governed by the auxiliary `package.json` files in the subtree.
import leaf from './aux/leaf.js';
import deep from './aux/deep/buried.js';
import flipBack from './aux/flip-back/mod.js';

export default { leaf, deep, flipBack };
