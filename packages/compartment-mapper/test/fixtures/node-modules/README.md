# Node modules fixtures

These package trees are shared by compartment-mapper's Node-facing tests and
other runtimes that need the same package-resolution and module-flavor cases.
Do not install their nested `node_modules` directories.
