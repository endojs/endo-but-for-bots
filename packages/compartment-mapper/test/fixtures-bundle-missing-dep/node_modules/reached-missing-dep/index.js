// This module requires a missing module at the top level, so the missing module
// is actually reached during evaluation. The bundle must still build, but
// evaluating it must throw the deferred error lazily, when the module is
// reached, mirroring the importer's runtime semantics.
const missing = require('./does-not-exist');

module.exports = { missing };
