// A non-Object-shaped export that, if it reached the unpatched
// preamble's `Object.defineProperty(...)` call, would cause the call to
// evaluate to `(() => null).defineProperty(...)` (undefined), throwing
// `TypeError` during functor evaluation.
export const Object = () => null;
