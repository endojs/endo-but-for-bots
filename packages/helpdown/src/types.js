// @ts-check

/**
 * A help record for one entity.
 *
 * Keys are method names; values are the documentation text for that method.
 * The empty-string key (`''`) holds the entity overview.
 *
 * @typedef {Record<string, string>} HelpText
 */

export {};
